spring:
  application:
    name: discovery-service

#   Eureka server configs. As this is itself a server, disabling the eureka clients' default configs.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 9000